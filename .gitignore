# ————— SECRETS : ne JAMAIS committer —————
# Clé privée de déchiffrement, clé service Supabase, mots de passe en clair, clé d'équipe
**/pegasus-private.pem
**/pegasus.config.json
**/clients.json
# Fichier-secret local nommé exactement « team-key » (ex : copie de ~/.pegasus/team-key).
# NB : pattern resserré pour ne PAS attraper make-team-key.mjs ni supabase/team-key-delivery.sql (non-secrets).
**/team-key
**/team_key
.env
*.env

# ————— Workspace local de dev (reste sur le Mac, hors dépôt) —————
# Contient les secrets ci-dessus + les helpers terminal + l'ancienne registration MCP.
/mcp-server/
/wordpress-plugin/

# ————— Divers —————
Logo/
**/*.ai
**/node_modules/
.DS_Store
**/.DS_Store

# ————— App desktop : artefacts de build (le .dmg est publié via Releases) —————
/desktop/dist/
/desktop/out/

# NB : le zip du plugin WP (pegasus/wordpress-plugin/pegasus.zip) EST versionné
# volontairement (distribution) — il ne contient que des clés publiques.

# ————— Fichiers LOCAUX contenant la clé d'équipe — NE JAMAIS committer —————
/*.txt
/*.rtf
Installer Pegasus - Guide.txt
Clé.txt
Cle.txt
